<h4>Search articles</h4><hr>
  <%= search_form_for @search do |f| %>
    <div class="fields"> 
      <% f.condition_fields do |c| %>
        <% c.attribute_fields do |a| %>
          <% a.attribute_select %>
        <% end %>
        <% c.predicate_select %>
        <% c.value_fields do |v| %>
          <% v.text_field :value %>
        <% end %>
        <br>
      <% end %>
      <%= f.label :title_cont, "Title: " %>
      <div class="input-group"> 
        <%= f.search_field :title_cont, class: "form-control", placeholder: "type article title here" %>
        <%= f.submit "🔍 Search", class: "btn btn-sm btn-outline-success" %>
      </div>
    </div>
  <% end %>
  <br>
<hr>
<%= link_to "Refresh all search", articles_path, class:"btn btn-primary" %>
<br>
<em>Showing <%= @articles.count %> article(s) out of <%= Article.all.count %></em>

